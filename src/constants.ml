let window_width = 1100

let window_height = 750

let square_size = 60

let square_spacing = 2

let square_offset = square_size + square_spacing

let board_size = square_offset * 8

let x_margin = (window_width - board_size) / 2

let y_margin = (window_height - board_size) / 2
