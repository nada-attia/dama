let x_margin = 100

let y_margin = 100

let square_size = 60

let square_spacing = 2

let square_offset = square_size + square_spacing

(* lower bound for row positions *)
let lower_8 = y_margin

let lower_7 = lower_8 + square_offset

let lower_6 = lower_7 + square_offset

let lower_5 = lower_6 + square_offset

let lower_4 = lower_5 + square_offset

let lower_3 = lower_4 + square_offset

let lower_2 = lower_3 + square_offset

let lower_1 = lower_2 + square_offset

(* lower bound for column positions *)
let lower_a = x_margin

let lower_b = lower_a + square_offset

let lower_c = lower_b + square_offset

let lower_d = lower_c + square_offset

let lower_e = lower_d + square_offset

let lower_f = lower_e + square_offset

let lower_g = lower_f + square_offset

let lower_h = lower_g + square_offset
